import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.base690f554506edf795e5d84121.app',
  appName: 'Yardit',
  webDir: 'dist',
  ios: {
    handleApplicationNotifications: false,
  },
};

export default config;
