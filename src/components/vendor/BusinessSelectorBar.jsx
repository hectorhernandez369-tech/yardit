import OrganizerAccountSwitcher from "@/components/organizer/OrganizerAccountSwitcher";

/**
 * Shared organizer account switcher with the original Vendor Dashboard header styling.
 */
export default function BusinessSelectorBar(props) {
  return <OrganizerAccountSwitcher {...props} />;
}